package com.vcredit.lee.interceptor;

public class MeterAdvice {// implements MethodInterceptor {

	/*
	 * @Autowired private MetricRegistry metricRegistry;
	 * 
	 * @Override public Object invoke(MethodInvocation invocation) throws Throwable
	 * {
	 * 
	 * MyMeter my = (MyMeter) AnnotationUtils.findAnnotation(invocation.getMethod(),
	 * MyMeter.class); System.out.println(my.value());
	 * 
	 * Meter meter = metricRegistry.meter(""); meter.mark();
	 * System.out.println("===============MyMeter=============="); return null; }
	 */
}
